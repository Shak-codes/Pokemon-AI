// Move.cpp
#include "move.h"

Move::Move(const std::string& name, int dmg, int pp)
    : name(name), dmg(dmg), pp(pp) {
  // The constructor's body is empty since all work is done by the member
  // initializer list.
}
